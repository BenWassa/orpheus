[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests
python_files = test_*.py
python_functions = test_*

# Show warnings during test run for visibility
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
