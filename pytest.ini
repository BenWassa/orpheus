[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests
python_files = test_*.py
python_functions = test_*

markers =
    slow: marks tests requiring transformer model loading

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
